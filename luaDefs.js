var luaLoop = `while (true) do
    
end`

var luaTypeOf = ` type(replaceme)`

var luaRandomNumber = "math.random(min,max)"

var luaFloor = "math.floor(replaceme)"

var luaPrint = "print(replaceme)"